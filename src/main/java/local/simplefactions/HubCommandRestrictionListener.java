package local.simplefactions;

import org.bukkit.event.Listener;

/**
 * No longer used. Hub is now a separate Velocity server.
 * Command restrictions were relevant when Hub was an in-world world on this server.
 */
public class HubCommandRestrictionListener implements Listener {
    // intentionally empty
}